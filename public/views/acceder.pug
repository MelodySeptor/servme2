doctype html
html
	head
		include ./head.pug

	body
		include ./header.pug

		//Ñigui Ñogui
		div(id="index-banner" class="parallax-container")
			div(class="section no-pad-bot")
				div(class="container")
					br
					br
					h1(class="header center teal-text text-lighten-2") Acceder
					div(class="row center")
						h5(class="header col s12 light") Servme: servicios de confianza en casa
					div(class="row center")
						h5(class="header col s12 light") ¿Qué deseas hacer hoy?
					br 
					br
			div(class="parallax")
				img(src="images/index_background1.jpg" alt="Index_Image_Background")

		br
		br
		div(class="container")
			div(clas="section")
				div(class="row")
					form(class="col s12" method="post" action="/peticionLogin")
						div(class="row")
							div(class="input-field col s6")
								i(class="material-icons prefix") account_circle
								input(id="usernameAcceder" type="text" class="validate" required="required")
								label(for="usernameAcceder") Usuario

							div(class="input-field col s6")
								i(class="material-icons prefix") lock
								input(id="passwordAcceder" type="password" class="validate" required="required")
								label(for="passwordAcceder") Contraseña

						div(class="row")
							div(class="input-field col s6")
								i(class="material-icons prefix") error_outline
								label
									a(href="/recuperarPass") He olvidado la contraseña
							div(class="input-field col s6")
								button(class="btn waves-effect waves-light" type="submit" name="action") Acceder
									i(class="material-icons right") chevron_right
		br
		include ./footer.pug